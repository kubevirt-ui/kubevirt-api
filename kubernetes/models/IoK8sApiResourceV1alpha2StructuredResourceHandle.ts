/* tslint:disable */
/* eslint-disable */
/**
 * Kubernetes
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: unversioned
 *
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import { exists } from '../runtime';
import {
  IoK8sApiResourceV1alpha2DriverAllocationResult,
  IoK8sApiResourceV1alpha2DriverAllocationResultFromJSON,
  IoK8sApiResourceV1alpha2DriverAllocationResultToJSON,
} from './';

/**
 * StructuredResourceHandle is the in-tree representation of the allocation result.
 * @export
 * @interface IoK8sApiResourceV1alpha2StructuredResourceHandle
 */
export interface IoK8sApiResourceV1alpha2StructuredResourceHandle {
  /**
   * NodeName is the name of the node providing the necessary resources if the resources are local to a node.
   * @type {string}
   * @memberof IoK8sApiResourceV1alpha2StructuredResourceHandle
   */
  nodeName?: string;
  /**
   * Results lists all allocated driver resources.
   * @type {Array<IoK8sApiResourceV1alpha2DriverAllocationResult>}
   * @memberof IoK8sApiResourceV1alpha2StructuredResourceHandle
   */
  results: Array<IoK8sApiResourceV1alpha2DriverAllocationResult>;
  /**
   * RawExtension is used to hold extensions in external versions.
   *
   * To use this, make a field which has RawExtension as its type in your external, versioned struct, and Object in your internal struct. You also need to register your various plugin types.
   *
   * // Internal package:
   *
   * 	type MyAPIObject struct {
   * 		runtime.TypeMeta `json:",inline"`
   * 		MyPlugin runtime.Object `json:"myPlugin"`
   * 	}
   *
   * 	type PluginA struct {
   * 		AOption string `json:"aOption"`
   * 	}
   *
   * // External package:
   *
   * 	type MyAPIObject struct {
   * 		runtime.TypeMeta `json:",inline"`
   * 		MyPlugin runtime.RawExtension `json:"myPlugin"`
   * 	}
   *
   * 	type PluginA struct {
   * 		AOption string `json:"aOption"`
   * 	}
   *
   * // On the wire, the JSON will look something like this:
   *
   * 	{
   * 		"kind":"MyAPIObject",
   * 		"apiVersion":"v1",
   * 		"myPlugin": {
   * 			"kind":"PluginA",
   * 			"aOption":"foo",
   * 		},
   * 	}
   *
   * So what happens? Decode first uses json or yaml to unmarshal the serialized data into your external MyAPIObject. That causes the raw JSON to be stored, but not unpacked. The next step is to copy (using pkg/conversion) into the internal struct. The runtime package's DefaultScheme has conversion functions installed which will unpack the JSON stored in RawExtension, turning it into the correct object type, and storing it in the Object. (TODO: In the case where the object is of an unknown type, a runtime.Unknown object will be created and stored.)
   * @type {object}
   * @memberof IoK8sApiResourceV1alpha2StructuredResourceHandle
   */
  vendorClaimParameters?: object;
  /**
   * RawExtension is used to hold extensions in external versions.
   *
   * To use this, make a field which has RawExtension as its type in your external, versioned struct, and Object in your internal struct. You also need to register your various plugin types.
   *
   * // Internal package:
   *
   * 	type MyAPIObject struct {
   * 		runtime.TypeMeta `json:",inline"`
   * 		MyPlugin runtime.Object `json:"myPlugin"`
   * 	}
   *
   * 	type PluginA struct {
   * 		AOption string `json:"aOption"`
   * 	}
   *
   * // External package:
   *
   * 	type MyAPIObject struct {
   * 		runtime.TypeMeta `json:",inline"`
   * 		MyPlugin runtime.RawExtension `json:"myPlugin"`
   * 	}
   *
   * 	type PluginA struct {
   * 		AOption string `json:"aOption"`
   * 	}
   *
   * // On the wire, the JSON will look something like this:
   *
   * 	{
   * 		"kind":"MyAPIObject",
   * 		"apiVersion":"v1",
   * 		"myPlugin": {
   * 			"kind":"PluginA",
   * 			"aOption":"foo",
   * 		},
   * 	}
   *
   * So what happens? Decode first uses json or yaml to unmarshal the serialized data into your external MyAPIObject. That causes the raw JSON to be stored, but not unpacked. The next step is to copy (using pkg/conversion) into the internal struct. The runtime package's DefaultScheme has conversion functions installed which will unpack the JSON stored in RawExtension, turning it into the correct object type, and storing it in the Object. (TODO: In the case where the object is of an unknown type, a runtime.Unknown object will be created and stored.)
   * @type {object}
   * @memberof IoK8sApiResourceV1alpha2StructuredResourceHandle
   */
  vendorClassParameters?: object;
}

export function IoK8sApiResourceV1alpha2StructuredResourceHandleFromJSON(
  json: any,
): IoK8sApiResourceV1alpha2StructuredResourceHandle {
  return IoK8sApiResourceV1alpha2StructuredResourceHandleFromJSONTyped(json, false);
}

export function IoK8sApiResourceV1alpha2StructuredResourceHandleFromJSONTyped(
  json: any,
  _ignoreDiscriminator: boolean,
): IoK8sApiResourceV1alpha2StructuredResourceHandle {
  if (json === undefined || json === null) {
    return json;
  }
  return {
    nodeName: !exists(json, 'nodeName') ? undefined : json['nodeName'],
    results: (json['results'] as Array<any>).map(
      IoK8sApiResourceV1alpha2DriverAllocationResultFromJSON,
    ),
    vendorClaimParameters: !exists(json, 'vendorClaimParameters')
      ? undefined
      : json['vendorClaimParameters'],
    vendorClassParameters: !exists(json, 'vendorClassParameters')
      ? undefined
      : json['vendorClassParameters'],
  };
}

export function IoK8sApiResourceV1alpha2StructuredResourceHandleToJSON(
  value?: IoK8sApiResourceV1alpha2StructuredResourceHandle | null,
): any {
  if (value === undefined) {
    return undefined;
  }
  if (value === null) {
    return null;
  }
  return {
    nodeName: value.nodeName,
    results: (value.results as Array<any>).map(
      IoK8sApiResourceV1alpha2DriverAllocationResultToJSON,
    ),
    vendorClaimParameters: value.vendorClaimParameters,
    vendorClassParameters: value.vendorClassParameters,
  };
}
