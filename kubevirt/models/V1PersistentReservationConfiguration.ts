export interface V1PersistentReservationConfiguration {
  /** Enabled controls the deployment of additional resources like the pr-helper container for enabling the use of the SCSI persistent reservation VMs, defaults to False. */
  enabled?: boolean;
}
